server:
  port: 51401

spring:
  application:
    name: cloud-admin
  profiles:
    active: dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      #配置中心
      config:
        server-addr: 127.0.0.1:8848
        #指定文件后缀
        file-extension: yml
        shared-configs: ${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension},application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
      #注册中心
      discovery:
        server-addr: ${spring.cloud.nacos.config.server-addr}