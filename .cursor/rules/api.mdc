---
alwaysApply: false
---
当生成或修改API接口时，需要同步生成对应的API文档，API文档按照模块分不同的文件，每个文件命令按照API的命名规范，模块+API.md。API 放到apiDocs目录中，文档格式如下：

- 接口名称
描述接口主要是做什公用的，用简单的话描述，比如获取用户基本信息，更新用户基本信息
- 接口地址
对应API文件的真实地址，比如 cloud-admin-service/user/info
- 请求方式 只允许使用POST请求
- 功能说明
对接口功能的描述
- 请求参数
以json格式先示例，然后按以下格式说明参数的用途
json
{
    "current" : 1,
    "size" : 10,
    "date": "2025-12-20"
｝

- 响应参数
参数含义：
| 参数名 | 类型 | 是否必填 | 字段说明 | 示例值 |
|---|---|:--|:--|:--|
| code | long | 是 | 页码（默认1） | 1 |
| msg | String | 是 | 每页数量（默认10） | 15 |
| data | Object | 否 | 返回值（一个对象） | {} |
其中data是个Object，可以是简单类的包装类，也可以是一个实体对象。如果是对象的话，需要输出各种详细参数信息。如下面的data.id, data.name。
参数示例json：
{
    "code": 0,
    "msg": "success",
    "data": {
        "id": 1,
        "name": "Cloud"
    }
}